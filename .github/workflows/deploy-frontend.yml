name: Deploy Frontend Only

on:
  push:
    branches:
      - master
    paths:
      - 'src/**'
      - 'public/**'
      - 'index.html'
      - 'package*.json'
      - 'vite.config.*'
      - '.github/workflows/deploy-frontend.yml'

jobs:
  build-and-deploy-frontend:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install and Build Frontend
        run: |
          npm install
          VITE_API_BASE_URL=${{ secrets.VITE_API_BASE_URL }} VITE_ADMIN_API_BASE_URL=${{ secrets.VITE_ADMIN_API_BASE_URL }} VITE_COS_BASE_URL=${{ secrets.VITE_COS_BASE_URL }} npm run build
          # Remove dynamic assets from dist to prevent overwriting backend-managed files
          rm -rf dist/vup-cover dist/vup-bg dist/vup-avatar

      - name: Upload to Tencent Cloud COS
        uses: zkqiang/tencent-cos-action@v0.1.0
        with:
          args: upload -r ./dist/ /
          secret_id: ${{ secrets.TENCENT_SECRET_ID }}
          secret_key: ${{ secrets.TENCENT_SECRET_KEY }}
          bucket: ${{ secrets.COS_BUCKET }}
          region: ${{ secrets.COS_REGION }}

      - name: Purge Tencent Cloud CDN Cache
        env:
          TENCENT_SECRET_ID: ${{ secrets.TENCENT_SECRET_ID }}
          TENCENT_SECRET_KEY: ${{ secrets.TENCENT_SECRET_KEY }}
          # Check if CDN_PATHS secret exists, otherwise use default domain
          CDN_PATHS: ${{ secrets.CDN_PATHS || 'https://ovodm.top/' }}
        run: |
          pip install tencentcloud-sdk-python
          python .github/scripts/purge_cdn.py
